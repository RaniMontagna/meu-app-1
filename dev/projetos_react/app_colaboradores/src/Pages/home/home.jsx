const Home = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: 'calc(100vh - 58px)',
      }}
    >
      <h2>Bem-vindo ao sistema de colaboradores :)</h2>
    </div>
  );
};

export default Home;
